'use strict';

/* ============================================================
   REMPLACÉ par last-follower.js + last-subscriber.js
   Ce fichier est conservé uniquement pour éviter une erreur 404
   si une ancienne config le référence encore.
   Il ne fait rien — les deux composants séparés sont chargés
   directement dans index.html.
   ============================================================ */

// window.LastEvents reste undefined — non utilisé dans script.js v2
